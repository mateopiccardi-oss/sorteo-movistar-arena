[build]
# Redeploya solo si cambian archivos de la app del sorteo (index.html, s/, _redirects).
# formulario_inscripcion.html y sorteo_script.gs NO disparan redeploy.
ignore = "git diff --quiet HEAD^ HEAD -- index.html s/ _redirects"
